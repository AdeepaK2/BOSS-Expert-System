{
  "name": "boss",
  "version": "1.0.0",
  "private": true,
  "description": "B0SS - Business Opportunity Screening System: a Prolog expert system with a browser interface.",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node test/run_tau.js && node test/run_ui.js && node test/run_coverage.js",
    "test:swi": "swipl test/run_swi.pl",
    "test:e2e": "node test/run_browser.js",
    "serve": "node build.js && npx --yes http-server web -p 8080 -c-1 -o"
  }
}
